[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "tskb"
version = "0.1.0"
description = "Tidal station keeping barbell simulation"
authors = [{name = "ChatGPT"}]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src", "."]
